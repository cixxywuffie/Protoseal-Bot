spring.application.name=ProtoSeal

bot.token=${BOT_TOKEN}

# Local profile: keeps the existing H2 database for development from the IDE.
spring.datasource.url=jdbc:h2:file:./data/kinksterbot
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
